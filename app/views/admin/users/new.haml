= error_messages_for 'user'

- form_for :user, @user, :url => admin_users_url do |f|
  %p
    = label_tag 'login'
  %br/
  = f.text_field :login

  = render :partial => 'form', :locals => {:f => f}
  = submit_tag 'Create'