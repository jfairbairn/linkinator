= render :partial => 'post', :locals => {:post=>@post}

- @post.replies.each do |post|
  = render :partial => 'post', :locals => {:post=>post, :hide_in_reply_to => true}
  
%h2 Post a reply!
= render :partial => 'post_form', :locals => {:post => @new_post}