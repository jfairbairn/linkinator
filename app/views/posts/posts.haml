- content_for :title do
  The latest!

- content_for :sidebar do
  %h3 Post something!
  = render :partial => 'post_form', :locals => {:post => nil}
  <a href="javascript:location.href='http://localhost:3000/new?post[content]='+encodeURIComponent(location.href)+'&post[title]='+encodeURIComponent(document.title)">Linkinate</a>

- @posts.each do |post|
  = render :partial => 'post', :locals => {:post => post}